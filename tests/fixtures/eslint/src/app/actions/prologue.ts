"use strict";
"use server";

// `"use server"` need not be the very first statement — anywhere in the
// directive prologue makes this a server-action module.
export async function archiveNote() {
  return undefined;
}
